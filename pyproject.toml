[project]
name = "openenv-email-triage"
version = "0.1.0"
description = "OpenEnv Email Triage Environment"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests"
]

[tool.openenv]
entrypoint = "app:app"
